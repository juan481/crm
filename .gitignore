# dependencies
node_modules/
.pnp
.pnp.js

# build output
.next/
out/
build/

# env (NUNCA subir al repo)
.env
.env.local
.env.production

# uploads locales
public/uploads/

# base de datos SQLite local
prisma/dev.db
prisma/dev.db-journal

# debug logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# misc
.DS_Store
*.pem
Thumbs.db

# credenciales que a veces se descargan/pegan sueltas en la carpeta del
# proyecto (ej. la clave JSON de una cuenta de servicio de Google) — nunca
# deben terminar en el repo, las credenciales reales viven como env vars.
*service-account*.json
crm-just-create-*.json

# vercel
.vercel
